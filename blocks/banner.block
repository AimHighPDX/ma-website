<section class="hero-banner">
   <div class="banner-filter"></div>
   {.section collection}
      {.section mainImage}
         <img src="{assetUrl}" alt="{title}">
      {.end}
      <div class="banner-text">
         <h1>{title}</h1>
      </div>      
   {.end}
</section>