<section class="hero-banner">
   {.section collection}
      {.section mainImage}
         <img src="{assetUrl}" alt="{title}">
      {.end}
      <div>
         <h1>{title}</h1>
      </div>      
   {.end}
</section>